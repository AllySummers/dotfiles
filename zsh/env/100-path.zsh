path=(
    "$HOME/.bin"
    "$HOME/.cargo/bin"
    "$DENO_INSTALL/bin"
    "$FNM_PATH"
    "$HOME/.ts-cli/bin"
    "$BUN_INSTALL/bin"
    "$HOME/.local/bin"
    # Jenv Shims
    "$JENV_ROOT/shims"
    # Pyenv Shims
    "$PYENV_ROOT/shims"
    "$PYENV_ROOT/bin"
    # Homebrew
    "$HOMEBREW_PREFIX/bin"
    "$HOMEBREW_PREFIX/sbin"
    "$HOMEBREW_PREFIX/opt/util-linux/sbin"
    "$HOMEBREW_PREFIX/opt/util-linux/bin"
    "$PNPM_HOME"
    "/opt/local/bin"
    "/opt/local/sbin"
    $path
    "$HOME/.micromamba/bin"
    "$HOME/micromamba/bin"
)
